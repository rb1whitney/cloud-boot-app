apiVersion: helm.crossplane.io/v1beta1
kind: Release
metadata:
  name: cloud-boot-app
  namespace: crossplane-system
spec:
  forProvider:
    chart:
      name: cloud-boot-app
      repository: oci://registry-1.docker.io/rb1whitney # Published at https://hub.docker.com/r/rb1whitney/cloud-boot-app
      version: "0.1.0"
    namespace: cloud-boot-app
    values:
      replicaCount: 3
      image:
        tag: "modernized"
      env:
        spring_profiles_active: "mysql,prod"
      service:
        type: LoadBalancer
      resources:
        limits:
          cpu: 1000m
          memory: 2Gi
        requests:
          cpu: 500m
          memory: 1Gi
  providerConfigRef:
    name: helm-provider-config
